/*“à•”Œ‹‡*/
select
 users.id,
 users.last_name,
 users. first_name,
 prefecture.name
from
 users 
inner join
 prefectures
on users,prefecture_id=prefectures.id;



select
 u.id,
 u.last_name,
 u. first_name,
 p.name
from
 users as u
inner join
 prefectures as p
on u,prefecture_id=p.id;

  /*as‚Æinner‚ÍÈ—ª‰ÂBæ‚Í’·‚¢‚Ì‚£*/


select
 u.id,
 u.last_name,
 u. first_name,
 p.name
from
 users as u
inner join
 prefectures as p
on u,prefecture_id = p.id
where u.gender = 2 ;

‹Lq‡˜

select
from
Œ‹‡ˆ—
where
group by
having
order by
limit
